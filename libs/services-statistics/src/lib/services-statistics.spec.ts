import { servicesStatistics } from "./services-statistics";

describe("servicesStatistics", () => {
  it("should work", () => {
    expect(servicesStatistics()).toEqual("services-statistics");
  });
});
